// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyDTjBUcdNnvlL0dY084sRAxr2mh-Hv8MF8",
    authDomain: "festivities-72ebe.firebaseapp.com",
    databaseURL: "https://festivities-72ebe.firebaseio.com",
    projectId: "festivities-72ebe",
    storageBucket: "festivities-72ebe.appspot.com",
    messagingSenderId: "817097069457"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
